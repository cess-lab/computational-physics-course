# Week 03 lecture-deck revision evidence

## Gate and generation

- User-approved scope: revise the existing Week 03 lecture deck from 14 to 15 slides while preserving existing uncommitted Week 03 work.
- Revised outline/assets: stored in `outline.md`, `deck_spec.json`, and `assets/strict/`; the canonical circuit asset was recompiled after adding the top-junction arrows.
- Approved sample: `origin_image/slide_02.png`, generated with the built-in `image_gen` edit path using the updated circuit and shared-current equation as strict context; work continued after the lecturer's `please continue` response.
- Slide jobs: `slide_jobs.json` records one accepted sample (`slide_02`) and 14 recorded worker results (`slide_01`, `slide_03`–`slide_15`).
- Dispatch provenance: each non-sample worker records its agent ID, nickname, prompt path/hash, built-in backend match, selected source hash, and final image hash. Worker model/reasoning overrides were omitted so the coordinator settings were inherited.

## Content QA

- Slide 02 merges prediction and shared-branch content and shows clockwise loops, `I1` entering the top junction, `I2` leaving to the right, `I_shared` leaving downward, `I1 = I2 + I_shared`, `I_shared = I1 − I2`, and the qualitative prediction `I1 > I2`.
- Slides 12–13 are explicitly optional future exposure and use three mesh currents with a symbolic 3×3 scaffold; Slide 13 contains no completed numerical matrix or step-by-step solution.
- Slide 14 separates representation reordering from source perturbation and preserves `maximum_reordering_difference_A = 0`, `V2: 2.0 V → 2.1 V`, `x_perturbed = [2.0091; 1.0273] A`, and `Δx = [0.0091; 0.0273] A`; residual/rank/conditioning/power-balance items are labelled optional stretch checks.
- Slide 15 uses a balanced 2×2 response grid and a compact transfer cue.
- The locked two-loop equations, values, units, current directions, MATLAB code, validation evidence, and wrong-sign evidence remain present in the core sequence.

## Assembly and package QA

- Final origin-image count: 15 (`slide_01.png` through `slide_15.png`), all inspected at full size.
- Final PPTX: `Lecture_Slides_Week03.pptx` in both the hidden canonical folder and `Week03/` student-facing folder.
- Assembly result: 15 slides, 15 notes sections, 15 media images, one full-slide picture per slide, 16:9 dimensions `9144000 × 5143500` EMU.
- Integrity: `unzip -t` and `officecli validate` passed; final SHA-256 is `17c3754b2f5a1e673598d5c0643ef6b7ceb68802ea6b47c87c0bc784fd81ad48`.
- Scope check: this pass changed the lecture-deck source/assets, generation records, QA evidence, assembled deck, and the student-facing README sentence describing the optional bridge exposure; the learning note, MATLAB Live Script, and practical were not edited during this pass.

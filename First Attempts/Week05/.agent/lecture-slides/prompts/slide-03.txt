ONE 1920×1080 ImageGen raster presentation slide, not a poster. Strict course visual master: flat white background #FFFFFF; no border/frame, no logo/footer/watermark/URL, no background pattern/texture/grid/gradient. Keep a clear empty 115 px left margin and 60 px top margin: no ink may occur closer than 76 px to any canvas edge.

Place sentence-case title at EXACT upper-left safe anchor (x=115, y=60) in bold condensed navy: Root finding starts with a residual
Place subtitle directly below x=115 y=142, charcoal, EXACTLY: A trial value becomes credible only when the mismatch is small for the right equation
Do not add periods. Never uppercase the title.

Left flat pale #F4F8FC rounded card: exact heading Implicit physical question ; exact centered formula f(x)=0 ; exact body f measures how far a trial value is from satisfying the model.
Right white outlined residual graph card: axes exact labels trial value x and residual f(x); exact small labels trial value, residual, root, mismatch. One smooth blue nonlinear curve MONOTONIC DECREASING from above to below the x axis. Exactly one axis crossing only, shown by one green root dot labelled root. A blue trial point on x axis to the left; a blue curve point directly vertically above it; a thin red vertical line labelled mismatch; the word residual near that vertical line.

Bottom x=115 y=930 rounded strip with pale #F4F8FC fill and thin navy stroke; navy text only. It must contain EXACTLY this single sentence and no prefix/heading/tag such as Takeaway: : Do not ask “what number did the solver return?” before asking “what residual did this number satisfy?”

Use final Week 02 and Week 03 title-led clean white scientific infographic language: sentence-case navy Roboto Condensed-like title, Roboto-like charcoal body, fine 2px card outlines, no heavy shadows. Ensure every written word is crisp and exactly as supplied. All content visible must be generated inside one single raster, no editable overlays, no SVG, HTML, code, placeholder, icon, or decorative imagery.

PHY4605 editable PowerPoint master proof

Opening master contract
- Course tag: exactly `PHY4605 Computational Methods in Physics` in Title Case at `(115, 126)`.
- Opening title: the individual deck title in Title Case; the master exemplar is `Physics to Arrays and Plots`, in the fixed `(115, 245, 1080, 210)` title box.
- Opening subtitle begins `Week N |` and continues with a short topic-specific tagline; the master exemplar is `Week 1 | From Motion to MATLAB` at `(115, 475, 1180, 80)`. The hero visual uses `(1305, 200, 420, 530)`; optional theme chain uses `(115, 825, 1500, 70)`.

Purpose
- Establish the approved opening-slide composition.
- Demonstrate T1, T2, and T3 content-slide title variants.
- Demonstrate Nunito body hierarchy and IBM Plex Mono code.
- Demonstrate project-local LaTeX-rendered display equations, cards, workflow graphics, and MATLAB-generated evidence figures.
- Demonstrate a generated visual asset without generated slide typography.

Scientific basis
- Constant-acceleration vertical motion, y(t) = y0 + v0*t - 0.5*g*t^2.
- Baseline values use y0 = 0 m, v0 = 20 m/s, and g = 9.81 m/s^2.
- Comparison values use v0 = 20 m/s and v0 = 15 m/s.

Visual provenance
- All five illustration roles were generated through OpenAI ImageGen on 2026-08-31 using the locked `PHY4605 Matte Scientific Cutaway` style: orthographic restrained 2.5D forms, white background, matte cobalt objects, navy technical linework, pale-blue guides, gold comparison/peak emphasis, teal validation emphasis, soft upper-left illumination, subtle contact shadows, and no embedded text. The current equation-to-motion and comparison assets are versioned vertical-motion replacements of the original generated files.
- The projectile illustration shows launch, peak, and return on one vertical guide.
- The sampled-motion strip shows eleven successive ball positions with dashed vertical guides, aligned to editable indices 1–11.
- The launch-boundary illustration shows the ball at the ground with upward initial velocity and downward gravity.
- The equation-to-motion illustration shows launch, rise, peak, descent, and return as a strictly one-dimensional vertical y(t) time sequence; it does not depict horizontal projectile travel.
- The vertical-motion comparison illustration contrasts a taller blue and shorter gold vertical y(t) sequence under the same downward gravity; it does not depict a horizontal coordinate or parabolic trajectory.
- Exact slide titles, labels, code, and annotations are deterministic editable PowerPoint objects. Standalone display equations, indexed expressions, symbol/subscript fragments, and symbol-plus-unit expressions use deterministic LaTeX assets with retained TeX/SVG/PNG source; surrounding prose and MATLAB code remain editable. Numerical figures are MATLAB PNG assets with retained MATLAB source/data and LaTeX-interpreted figure text.
- The equation asset set was rendered with the installed `render-equations-diagrams` workflow on 2026-08-31 at PNG scale 6. It includes the position model, array-moment expressions, launch and validation checks, symbol/unit entries, equation terms, and comparison parameter labels. Transparent PNG fallbacks are inserted after retaining matching SVG and editable TeX files beside the deck source.
- Every equation-bearing slide note includes a standardized `[Equation provenance]` block with the exact TeX, retained SVG/PNG paths, foreground colour, target box size, `contain` fit, alt text, and QA/render provenance.
- The exported PPTX is post-processed by `repairExportedPptx` to persist semantic `p:cNvPr@name` and `p:cNvPr@descr` for all images; the deterministic order map is required because the artifact exporter may emit generic image names. The repair also normalizes major/minor Nunito declarations across every `ppt/**/theme*.xml` part.

Review boundary
- This is a design-system proof, not the Week 1 lecture deck.
- Week 1 production should begin only after lecturer approval of this proof.
- The editable master is generated by Terra Medium (`gpt-5.6-terra`, `medium`) and audited read-only by Sol Medium (`gpt-5.6-sol`, `medium`), with at most three rounds and a hidden QA ledger.

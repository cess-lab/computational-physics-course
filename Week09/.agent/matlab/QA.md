# Week 09 Lecture Live Script QA

The student-facing lecture demonstration was checked with MATLAB Code Analyzer and executed from top to bottom in a separate MATLAB R2026b Prerelease Update 3 process. Code Analyzer reported only intentional unsuppressed outputs used for teaching; it reported no error or warning requiring repair.

Fresh-process validation is retained in `lecture_validation.log`. The MATLAB launcher printed path-related startup warnings before MATLAB opened, but the batch process completed with exit code 0 and emitted `WEEK09_LECTURE_FRESH_RUN_PASSED`.

Validated numerical evidence:

- fitted resistance: `47.0000 ohm`;
- fitted offset: `0.0025 V`;
- residual RMSE: `0.0129903811 V`;
- largest absolute residual: `0.0225 V`;
- nominal 47 ohm, 5% range: `44.65–49.35 ohm`, with the fitted value inside it;
- repeat-run fitted slopes: approximately `47.00`, `47.06`, and `47.27 ohm`, giving the deliberately simple `47.0–47.3 ohm` repeatability span.

The strict plots and CSV evidence were regenerated by `generate_week09_reference_assets.m`, which emitted `WEEK09_REFERENCE_ASSETS_PASSED`. The lecture uses base MATLAB `polyfit`/`polyval`; no fitting toolbox is required.

No separate ImageGen illustration is justified in the Live Script. The teaching barrier is the mapping among measured points, fitted predictions, and residuals, which the native MATLAB graphs expose more precisely.

Source-format checks passed: exactly one H1, no heading deeper than H3, exactly one blank line immediately before the plain-text Live Script appendix, and no `.mlx` student-facing copy.
